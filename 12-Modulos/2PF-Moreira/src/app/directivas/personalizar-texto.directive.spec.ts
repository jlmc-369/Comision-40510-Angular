import { PersonalizarTextoDirective } from './personalizar-texto.directive';

describe('PersonalizarTextoDirective', () => {
  it('should create an instance', () => {
    const directive = new PersonalizarTextoDirective();
    expect(directive).toBeTruthy();
  });
});
