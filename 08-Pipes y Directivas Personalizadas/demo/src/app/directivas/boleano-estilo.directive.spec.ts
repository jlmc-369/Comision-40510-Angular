import { BoleanoEstiloDirective } from './boleano-estilo.directive';

describe('BoleanoEstiloDirective', () => {
  it('should create an instance', () => {
    const directive = new BoleanoEstiloDirective();
    expect(directive).toBeTruthy();
  });
});
