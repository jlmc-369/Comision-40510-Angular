import { BoleanoATextoPipe } from './boleano-atexto.pipe';

describe('BoleanoATextoPipe', () => {
  it('create an instance', () => {
    const pipe = new BoleanoATextoPipe();
    expect(pipe).toBeTruthy();
  });
});
